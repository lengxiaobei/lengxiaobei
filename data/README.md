# Runtime Data

This directory is the canonical local-first runtime storage root.

- `sqlite/agent.db`: primary SQLite database
- `chroma/`: vector database persistence
- `skills/`: generated YAML skills awaiting review
- `logs/`: JSONL application logs
- `uploads/`: user uploaded files
