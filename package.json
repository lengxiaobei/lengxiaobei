{
  "name": "lengxiaobei-workspace",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "uvicorn backend.main:app --reload --host ${BACKEND_HOST:-127.0.0.1} --port ${BACKEND_PORT:-8000}",
    "frontend": "npm run dev --prefix frontend",
    "test": "pytest backend/tests tests -q",
    "check": "python3 -m compileall -q backend && npm run build --prefix frontend"
  },
  "workspaces": [
    "frontend"
  ]
}
