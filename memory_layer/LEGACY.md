# memory_layer — LEGACY (Rust)

> **已废弃** — 此 Rust memory_layer 已被 Python `src/hybrid_memory.py` + `src/memory_tree.py` 替代。

原功能：独立 Rust 记忆服务，使用 `memories.json` 和简单字符频率 embedding。

迁移至: Python SQLite / HybridMemory / MemoryTree 四层记忆模型。

保留原因: 历史参考。数据文件 `memories.json` 如需迁移请联系维护者。
如无维护者，可归档至 `docs/archived/`。
