# Core backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.25.0
pyyaml>=6.0.0
sqlite-utils>=3.36

# Optional YourAgent adapters (install with `pip install -e .[full]`)
# chromadb>=0.5.0       # OpenHuman-style persistent vector store
# playwright>=1.40.0    # OpenClaw-style browser tool; run `playwright install chromium`
# apscheduler>=3.10.0   # Optional scheduler backend; lightweight fallback is built in
