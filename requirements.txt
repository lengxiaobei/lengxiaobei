# Leng Xiaobei Phase 1 Dependencies
# Minimal — no heavy ML libraries, pure Python

# Core
requests>=2.31.0  # HTTP calls (SearXNG search, web fetch)
jsonschema>=4.19.0  # JSON Schema validation
flask>=2.0.0  # Web interface
psutil>=5.9.0  # System monitoring

# Optional: for vector embeddings (Phase 2)
# sentence-transformers>=2.0
# numpy>=1.24

# Optional: for LLM calls (Phase 2)
# openai>=1.0
